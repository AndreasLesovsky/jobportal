export interface ApiConfig {
  baseUrl: string;
  checkActivity: string;
  deleteEntity: string;
  downloadCv: string;
  getApplicants: string;
  getJobs: string;
  getUsers: string;
  login: string;
  logout: string;
  saveJob: string;
  saveUser: string;
  sendContactMail: string;
  sendJobApplication: string;
  toggleEntity: string;
}